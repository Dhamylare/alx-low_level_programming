#include "main.h"

/**
* _isupper - checks if character is uppercase
* @c: character being checked
* Return: Return 1 if c is upper and 0 for anything else
*/
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);

}
