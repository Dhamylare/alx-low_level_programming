#include "main.h"
/**
* _isdigit - checks for digit
* @c: number being checked
* Return: return 1 if its digit and 0 if anything else
*/
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
	return (1);
	}
	return (0);
}
