#include "main.h"

/**
* _islower - check if islower
* @c: variable being checked for case
* Return: 1 if lower or 0 otherwise
*/

int  _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
		return (0);

}
